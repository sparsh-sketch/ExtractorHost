# overwrite the file with a valid config
cat > vercel.json <<'JSON'
{
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.11",
      "maxDuration": 60
    }
  }
}
JSON

git add vercel.json
git commit -m "fix: set functions runtime to python3.11"
git push
